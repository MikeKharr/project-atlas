{
  "name": "project-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check",
    "test": "node --test test/*.test.js"
  }
}
